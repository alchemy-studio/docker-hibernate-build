version: '3'
services:
  default:
    image: weli/docker-hibernate-build
#    restart:
    volumes:
      - /data:/data
    environment:
      DB_HOST: foo
      DB_USER: foo
      DB_PASSWORD:
    ports:
      - 8000:80
      - 9001:9001
